package com.layth;public class Main {    public static void main(String[] args) {	    // equal and plus operator        // literal value        int literalValue = 2;        // what ever to right is evualted and assigned to var        int result = 1 + 2;        System.out.println("1 + 2 " + result);        // single equal -> assignment operator        boolean isAlien = false;        //        if (isAlien == true) {            System.out.println("It is not an Alien");        }        int topScore = 80;        if(topScore < 100) {            System.out.println("You got the high score");        }    }}